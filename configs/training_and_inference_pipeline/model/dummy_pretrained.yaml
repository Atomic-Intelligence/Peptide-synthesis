_target_: tests.mock.dummy_model.DummySynthetizationModel.load_pretrained_from_mlflow_run

ml_flow_info:
  _target_: src.models.synthetization_model_interface.MlFlowTrainingRunInfo
  experiment_name: ${experiment_name}
  run_name: ${run_name}